var a = [["ciudad,pais"], [3,4]]
alert(a.length)

// muestra la longitud de los elementos de la variable a

var a = [["ciudad,pais"], [3,4]];
alert(a[1].length);

// muestra la longitud del segundo elemento de la variable a
